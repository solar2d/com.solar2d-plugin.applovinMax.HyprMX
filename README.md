Sources for the plugin `plugin.applovinMax.HyprMX`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.HyprMX" = {
            publisherId = "com.solar2d",
        },
    },
}
```
